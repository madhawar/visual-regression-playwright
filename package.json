{
  "devDependencies": {
    "@percy/cli": "1.0.5",
    "@percy/playwright": "1.0.2",
    "@playwright/test": "^1.40.1",
    "@types/node": "^20.10.1",
    "playwright": "^1.40.1"
  },
  "scripts": {
    "test": "playwright test demo --reporter=dot",
    "demo": "percy exec -- playwright test demo --reporter=dot",
    "percy": "percy exec -- playwright test percy --reporter=dot"
  }
}
